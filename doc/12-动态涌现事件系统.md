# 12 — 动态涌现事件系统

> 核心原则：世界发生的一切都必须有因果链。没有"凭空出现"的事件。

## 一、设计原则

### 1.1 禁止随机事件

**❌ 错误做法（当前）：**
- 远方消息从预设数组随机选
- NPC行为从模板数组随机选
- 事件没有因果依据

**✅ 正确做法：**
- 事件必须由世界状态变化触发
- NPC行为必须由其状态（饥饿/疲劳/铜钱）驱动
- 消息必须由真实事件传播而来
- 远方消息必须有L2统计支撑

### 1.2 三层因果架构

```
世界状态变化（L2统计 / L1行为 / L0 GOAP）
    ↓ 触发条件检查
事件生成（基于状态差异，    ↓ 传播链
叙事描述（基于事件内容
```

### 1.3 NPC 知识来源规则

NPC 只能谈论他们**实际感知**到的信息：
- 同一 Grid 内发生的事件（亲眼看到/听到）
- 从其他 NPC 传播来的消息（传播链）
- 在茶馆/集市等社交场所获取的（信息集散地）
- **绝对不能**谈论没有任何依据的事情

## 二、事件触发系统

### 2.1 天气因果链

| 天气状态 | 触发事件 | NPC反应 | 经济影响 |
|--------|----------|----------|----------|
| 小雨 | 行人避雨 | 户外NPC移向室内 | 伞销量↑ |
| 暴雨 | 街道冷清 | 几乎所有户外NPC回家 | 户外商品滞销 |
| 大暴雨 | 可能洪涝 | NPC恐慌 | 粮价暴涨 |
| 连续晴 | 丰收预期 | NPC心情好 | 粮价微跌 |
| 大雪 | 路面难行 | 出行减少 | 燃料涨价 |

**天气→NPC行为规则：**
- 暴雨时：户外NPC `go_home`; 商贩 `close_shop`; 茶馆NPC增多（避雨客涌入）
- 晴天时：正常行为; 街上NPC增多

### 2.2 经济因果链

```
L2农田产量 → 粮食供给 → 粮价
  供给 > 需求 × 1.2 → 粮价上涨
  供给 < 需求 × 0.8 → 粮价下跌

L2山林猎物 → 肉类供给 → 肉价
L2河流鱼获 → 鱼价
NPC交易行为 → 局部供需变化 → 短期价格波动
```

**经济→NPC行为规则：**
- 粮价 > 200文/石: NPC买不起粮，饥饿加速，可能偷窃/乞讨
- 布价低: NPC多买布，心情上升
- 热门商品短缺: 商贩囤货，玩家买不到

### 2.3 人口因果链

| 状态变化 | 触发事件 | 传播方式 |
|----------|----------|----------|
| 某区域人口变化>10% | 人口流动事件 | 茶馆传播 |
| 大量流民涌入 | 治安事件 | 官府反应 |
| 商人集中 | 市场繁荣 | 物价波动 |

### 2.4 生态因果链

```
狼群减少 → 兔子增多 → 菜地被啃 → 农田减产 → 粮价上涨
猎户增多 → 猎物减少 → 肉价上涨 → NPC抱怨
过度捕鱼 → 鱼群减少 → 渔夫收入下降 → 渔夫转行
```

### 2.5 NPC行为因果链

| NPC状态 | 触发行为 | 连锁效果 |
|--------|----------|----------|
| 饥饿<30 | 去买粮/乞讨/偷窃 | 粮铺生意好/治安事件 |
| 疲劳>80 | 去休息/回家 | 店铺暂时无人 |
| 铜钱<20 | 去打工/借钱/偷窃 | 码头劳力增加 |
| 铜钱>200 | 消费/进货/投资 | 市场繁荣 |
| 心情<30 | 去茶馆/找人聊天 | 茶馆生意好/传播消息 |

### 2.6 具体NPC因果链（10个核心NPC）

**王掌柜（布庄商贩）：**
- 布价低 → 进货（铜钱减少，库存增加）
- 有库存 → 开铺卖货（铜钱增加，库存减少）
- 库存=0 → "今天布卖完了" → 玩家买不到布
- 暴雨 → 关店 → 玩家进不了布庄
- 赚到钱(铜钱>200) → 去茶馆消费 → 传播商业消息

**刘寡妇（茶馆老板）：**
- 茶馆有客人 → 卖茶赚钱 → 传播最新消息（她是信息集散地）
- 知道某条消息 → 主动告诉玩家（"客官，您听说了吗？"）
- 天气差 → 客人少 → 收入减少 → 心情差
- 听到粮价上涨 → "最近日子不好过啊"

**码头管事：**
- 有船到港 → 分配搬运工作 → 玩家可以打工
- 没船 → "今天没活干" → 玩家不能打工
- 大船到港 → 搬运量大 → 工钱翻倍 → 消息传播"码头来了好大一条船"

**张三（混混/小偷）：**
- 饥饿<30且铜钱<10 → 偷窃（成功率基于附近NPC数量和衙役位置）
- 偷窃成功 → 被偷NPC铜钱减少 → 报官事件
- 被抓 → 关押 → 几回合不能行动 → 释放后声誉差
- 在茶馆 → 传播小道消息（不可靠信息，可能夸大）

**钱捕头（衙役）：**
- 张三偷窃 → 追捕 → 如果在同一Grid则立即抓
- 巡逻中 → 发现可疑行为 → 事件记录
- 城内治安好 → NPC心情上升
- 城内治安差 → NPC不敢出门 → 经济下滑

**李大夫（药铺）：**
- 有人生病 → 看病 → 收费 → 赚钱
- 药材充足 → 正常看病
- 药材短缺 → "药涨价了" → 无法看病 → 病情加重
- 城外有瘟疫 → 大量病人涌入 → 忙不过来 → 求助事件

**赵秀才（书生）：**
- 在贡院读书 → 才学增加
- 才学>阈值 → 可以参加科举 → 消息传播"听说下个月要加开一次恩科"
- 参加科举 → 中榜/落榜 → 命运改变
- 在茶馆 → 讨论时政/科举 → 传播消息

**陈猎户（猎户）：**
- 山里猎物多 → 打猎 → 获得猎物 → 肉价可能下降
- 山里猎物少 → "最近山里打不到东西" → 买不到肉
- 天气差 → 不进山 → 无猎物
- 受伤 → 去找李大夫 → 触发看病事件

**孙铁匠（铁匠）：**
- 有材料 → 锻造 → 产出铁器 → 卖掉赚钱
- 材料不足 → "最近矿石不好收" → 铁器涨价
- 暴雨 → 炉子灭 → 无法锻造
- 有大订单(如官府) → 加班 → 疲劳增加 → 需要休息

**周嫂子（农妇）：**
- 农田需要劳力 → 干活 → 产量增加
- 天气好 → 产量好 → 心情好 → 传播好消息
- 天气差 → 产量差 → "今年收成不好" → 粮价上涨消息
- 家人饿了 → 去买粮 → 触发经济交易

## 三、远方消息生成规则

### 3.1 远方消息必须有L2统计支撑

```
不能随机生成"陕西大旱"——除非L2统计中陕西区域确实干旱
不能随机生成"黄河决口"——除非L2统计中降雨量确实超标
不能随机生成"大船到港"——除非L2统计中确实有贸易船只到达
```

### 3.2 远方消息生成逻辑

```
每回合检查所有L2区域统计变化:
  for each region:
    if region.rainfall > threshold:
      generateEvent("flood_risk", region)
    if region.harvest < normal * 0.5:
      generateEvent("harvest_failure", region)
    if region.trade_volume > normal * 2:
      generateEvent("trade_boom", region)
    if region.animal_population < min_threshold:
      generateEvent("ecological_crisis", region)
```

### 3.3 消息传播规则

```
事件产生后:
1. 立即在发生区域可见
2. 1时辰后传播到相邻区域
3. 1天后传播到同城
4. 3天后通过商人传播到远方
5. 每次传播有20%概率细节丢失/夸大
```

## 四、NPC对话因果链

### 4.1 NPC只能说知道的事

```typescript
function generateNPCDialog(npc: NPC): string {
  // 检查NPC的记忆库
  const memories = npc.memory.recentEvents;
  
  if (memories.length === 0) {
    return null; // 这个NPC没什么可说的
  }
  
  // 根据NPC职业和性格筛选相关记忆
  const relevantMemories = memories.filter(m => isRelevant(npc.profession, m));
  
  if (relevantMemories.length === 0) {
    return null;
  }
  
  // 基于真实记忆生成对话
  return generateDialogFromMemory(npc, relevantMemories[0]);
}
```

### 4.2 对话模板基于事实

**❌ 错误：** "听说最近粮价要涨了"（随机）
**✅ 正确：**
- L2统计显示南郊农田产量下降30%
- → 经济系统：粮价上涨15%
- → 事件：粮价上涨
- → 传播到茶馆
- → 刘寡妇知道后说："最近南边遭了虫灾，今年新米怕是要涨价喽。"

**❌ 错误：** "码头来了好大一条船"（随机）
**✅ 正确：**
- L2统计显示河流区域贸易量增加200%
- → 事件：大型商船到港
- → 码头管事知道：分配20个搬运工
- → 传播到茶馆
- → 小孩跑过喊："快去看！码头来了好大一条船！"

## 五、实现步骤

### Phase 1: 天气系统
- 添加天气状态机到 timeSystem.ts
- 天气影响NPC行为、经济、L2统计

### Phase 2: 因果事件引擎
- 新文件 src/world/eventEngine.ts
- 每回合检查世界状态差异
- 基于差异生成因果事件

### Phase 3: NPC知识系统
- NPC记忆库存储真实感知到的事件
- NPC对话基于记忆库而非随机模板

### Phase 4: 经济因果
- 供需系统基于L2统计
- 价格变动基于真实供需比

### Phase 5: 移除所有随机事件
- 删除硬编码的消息池
- 所有事件由因果引擎生成
